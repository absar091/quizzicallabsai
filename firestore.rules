
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This app uses Firebase Realtime Database, not Firestore
    // These rules are kept for potential future Firestore usage
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
